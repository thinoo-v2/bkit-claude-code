# bkit Context Engineering 심층 분석 보고서

> **Version**: 1.0.0
> **Date**: 2026-01-25
> **Author**: Claude Opus 4.5
> **Scope**: bkit 플러그인 전체 코드베이스 Context Engineering 분석

---

## Executive Summary

bkit (Vibecoding Kit)은 Claude Code 플러그인 및 Gemini CLI Extension으로, **Context Engineering** 기법을 체계적으로 적용하여 AI-Native 개발 워크플로우를 지원합니다. 본 보고서는 bkit의 7개 핵심 디렉토리(agents/, commands/, hooks/, lib/, scripts/, skills/, templates/)를 심층 분석하여 각 컴포넌트가 구현하는 Context Engineering 기법을 문서화합니다.

### 핵심 발견사항

| 영역 | 파일 수 | Context Engineering 기법 |
|------|---------|-------------------------|
| **agents/** | 11개 | Role definition, Instruction structuring, Multi-language triggers |
| **commands/** | 21+20 (TOML) | Declarative prompts, PDCA workflow integration |
| **hooks/** | 3개 | Event-driven context injection, Platform detection |
| **lib/** | 1개 (2776줄) | 76 utility functions, Intent detection, Caching |
| **scripts/** | 26개 | Pre/Post hooks, Phase automation, QA monitoring |
| **skills/** | 18개 | Domain knowledge structuring, Level-based adaptation |
| **templates/** | 20개 | Document scaffolding, Cross-reference system |

---

## Part 1: Context Engineering 이론적 배경

### 1.1 Context Engineering의 정의

**Context Engineering**은 "LLM 추론 시 최적의 토큰 집합을 큐레이션하고 유지하는 전략의 집합"입니다 (Anthropic, 2025).

```
┌─────────────────────────────────────────────────────────────────┐
│                    Context Engineering                           │
│                                                                   │
│   Prompt Engineering의 진화된 형태로, 다중 추론 턴에 걸쳐         │
│   시스템 지시사항, 도구, 외부 데이터, 메시지 히스토리를           │
│   포함한 전체 컨텍스트 상태를 관리                                │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 핵심 원칙

| 원칙 | 설명 | bkit 적용 |
|------|------|-----------|
| **최소 고신호 토큰** | 원하는 결과를 최대화하는 최소한의 고품질 정보 | 레벨별 문서 분기 |
| **컨텍스트 부패 방지** | 토큰 증가 시 정확도 저하 현상 대응 | Compaction, Sub-agent |
| **적절한 고도 찾기** | 너무 구체적이지도 모호하지도 않은 지시사항 | Phase별 상세도 조절 |
| **도구 설계 원칙** | 명확하고 중복 없는 도구 세트 | 역할별 Agent 분리 |

### 1.3 주요 기법

```
┌─────────────────────────────────────────────────────────────────┐
│                  Context Engineering 기법 분류                    │
├─────────────────┬───────────────────────────────────────────────┤
│ System Prompts  │ 역할 정의, 행동 규칙, 출력 포맷 지정           │
├─────────────────┼───────────────────────────────────────────────┤
│ Few-shot        │ 대표 예제로 기대 행동 시연                     │
├─────────────────┼───────────────────────────────────────────────┤
│ Chain-of-Thought│ 단계별 추론 유도 ("Let's think step by step") │
├─────────────────┼───────────────────────────────────────────────┤
│ RAG             │ 외부 지식 검색 및 주입                        │
├─────────────────┼───────────────────────────────────────────────┤
│ Tool/Function   │ 환경 상호작용 및 정보 검색 도구               │
├─────────────────┼───────────────────────────────────────────────┤
│ Memory Mgmt     │ Compaction, Note-taking, Sub-agent            │
└─────────────────┴───────────────────────────────────────────────┘
```

---

## Part 2: bkit 아키텍처 개요

### 2.1 디렉토리 구조

```
bkit-claude-code/
├── agents/          # 11개 전문 에이전트 정의
├── commands/        # 21개 슬래시 커맨드 (+ Gemini TOML 20개)
├── hooks/           # 이벤트 훅 시스템 (SessionStart, Pre/PostToolUse)
├── lib/             # 공통 유틸리티 (2776줄, 76개 함수)
├── scripts/         # 26개 자동화 스크립트
├── skills/          # 18개 도메인 지식 스킬
├── templates/       # 20개 PDCA/Pipeline 문서 템플릿
└── docs/            # 문서화 (01-plan, 02-design, 03-analysis, 04-report)
```

### 2.2 핵심 개념

#### PDCA 사이클
```
┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐
│  Plan   │───▶│ Design  │───▶│   Do    │───▶│  Check  │───▶│   Act   │
│ (계획)  │    │ (설계)  │    │ (구현)  │    │ (검증)  │    │ (개선)  │
└─────────┘    └─────────┘    └─────────┘    └─────────┘    └─────────┘
     │              │              │              │              │
     ▼              ▼              ▼              ▼              ▼
pdca-plan      pdca-design    구현 작업     pdca-analyze   pdca-report
                                             (gap-detector) (pdca-iterator)
```

#### 9-Phase Development Pipeline
```
Phase 1: Schema/Terminology  ─┬─ 공통 기반
Phase 2: Coding Convention   ─┘
Phase 3: Mockup Development  ─┬─ 설계 단계
Phase 4: API Design/Impl     ─┤
Phase 5: Design System       ─┘
Phase 6: UI Implementation   ─┬─ 구현 단계
Phase 7: SEO/Security        ─┘
Phase 8: Review              ─── 검증 단계
Phase 9: Deployment          ─── 배포 단계
```

#### 3-Level 프로젝트 분류
```
┌────────────┬────────────────────┬────────────────────────────────┐
│ Level      │ 대상               │ 기술 스택                      │
├────────────┼────────────────────┼────────────────────────────────┤
│ Starter    │ 초보자/비개발자    │ HTML/CSS/JS, Next.js 기초      │
│ Dynamic    │ 풀스택 개발자      │ Next.js + bkend.ai BaaS        │
│ Enterprise │ 시니어/아키텍트    │ Microservices, K8s, Terraform  │
└────────────┴────────────────────┴────────────────────────────────┘
```

---

## Part 3: agents/ 디렉토리 분석

### 3.1 개요

| 항목 | 내용 |
|------|------|
| 파일 수 | 11개 |
| 형식 | Markdown with YAML frontmatter |
| 목적 | 특화된 역할의 Sub-agent 정의 |

### 3.2 에이전트 분류

```
┌─────────────────────────────────────────────────────────────────┐
│                       bkit Agents 분류                          │
├───────────────┬─────────────────────────────────────────────────┤
│ Expert        │ bkend-expert, enterprise-expert, infra-architect│
│ (전문가)      │ 특정 도메인의 깊은 지식 제공                    │
├───────────────┼─────────────────────────────────────────────────┤
│ Analysis      │ code-analyzer, design-validator, gap-detector   │
│ (분석)        │ 코드/문서 품질 검증 (읽기 전용)                 │
├───────────────┼─────────────────────────────────────────────────┤
│ Automation    │ pdca-iterator, qa-monitor, report-generator     │
│ (자동화)      │ 반복 작업 자동화                                │
├───────────────┼─────────────────────────────────────────────────┤
│ Guide         │ pipeline-guide, starter-guide                   │
│ (가이드)      │ 사용자 친화적 안내                              │
└───────────────┴─────────────────────────────────────────────────┘
```

### 3.3 에이전트별 상세 분석

| Agent | Model | Allowed Tools | 주요 Context Engineering 기법 |
|-------|-------|---------------|------------------------------|
| **bkend-expert** | (default) | Read, Write, Edit, Glob, Grep, mcp__bkend__* | BaaS 도메인 지식, MCP 통합 |
| **code-analyzer** | opus | Glob, Grep, Read, LSP, Task | 읽기 전용 제약, LSP 통합 |
| **design-validator** | opus | Glob, Grep, Read | 읽기 전용 문서 검증 |
| **enterprise-expert** | opus | Read, Write, Edit, Glob, Grep, Task, WebSearch | AI Native 방법론, Monorepo |
| **gap-detector** | opus | Read, Glob, Grep, Task | Match Rate 계산, 임계값 기반 |
| **infra-architect** | opus | Read, Write, Edit, Glob, Grep, Bash, Task | IaC 패턴, K8s/Terraform |
| **pdca-iterator** | sonnet | Read, Write, Edit, Glob, Grep, Bash, Task, TodoWrite, LSP | Evaluator-Optimizer 패턴 |
| **pipeline-guide** | sonnet | Glob, Grep, Read, TodoWrite | 9-Phase 가이드 |
| **qa-monitor** | haiku | Bash, Read, Write, Glob, Grep, Task | Zero Script QA, Docker logs |
| **report-generator** | haiku | Read, Write, Glob, Grep | KPT 회고, 요약 생성 |
| **starter-guide** | sonnet | Read, Write, Edit, Glob, Grep, WebSearch, WebFetch | 초보자 친화적 설명 |

### 3.4 Context Engineering 기법 상세

#### 3.4.1 역할 정의 (Role Definition)
```markdown
# gap-detector.md 예시
You are a Gap Analysis expert. Your job is to detect gaps between
design documents and actual implementation to maintain consistency.
```

#### 3.4.2 다국어 트리거 (Multi-language Triggers)
```yaml
triggers:
  - gap analysis, design-implementation check, compare design
  - 갭 분석, 설계-구현 비교, 검증, 확인
  - ギャップ分析, 設計検証
  - 差距分析, 对比设计
  - est-ce correct?, ist das richtig?
```

#### 3.4.3 임계값 기반 분기 (Threshold-based Branching)
```markdown
### Match Rate Decision Matrix

| Match Rate | Action |
|------------|--------|
| < 70%      | Major Gap Report + Immediate Fix |
| 70-89%     | Gap Analysis Report + pdca-iterator |
| >= 90%     | Brief Summary + Complete |
```

#### 3.4.4 읽기 전용 제약 (Read-only Constraint)
```yaml
# code-analyzer, design-validator
allowed_tools:
  - Glob
  - Grep
  - Read
  # Write, Edit 의도적 제외
```

---

## Part 4: commands/ 디렉토리 분석

### 4.1 개요

| 항목 | 내용 |
|------|------|
| 파일 수 | 21개 (Markdown) + 20개 (Gemini TOML) |
| 형식 | Markdown with YAML frontmatter / TOML |
| 목적 | 슬래시 커맨드 정의 (/pdca-plan, /init-starter 등) |

### 4.2 커맨드 분류

```
┌─────────────────────────────────────────────────────────────────┐
│                      bkit Commands 분류                          │
├───────────────┬─────────────────────────────────────────────────┤
│ PDCA          │ pdca-plan, pdca-design, pdca-analyze,           │
│               │ pdca-iterate, pdca-report, pdca-status, pdca-next│
├───────────────┼─────────────────────────────────────────────────┤
│ Pipeline      │ pipeline-start, pipeline-status, pipeline-next  │
├───────────────┼─────────────────────────────────────────────────┤
│ Init          │ init-starter, init-dynamic, init-enterprise     │
├───────────────┼─────────────────────────────────────────────────┤
│ Setup         │ setup-claude-code, learn-claude-code,           │
│               │ upgrade-claude-code, upgrade-level              │
├───────────────┼─────────────────────────────────────────────────┤
│ Utility       │ archive, github-stats, zero-script-qa           │
└───────────────┴─────────────────────────────────────────────────┘
```

### 4.3 플랫폼별 출력 형식

```
┌──────────────────────┬──────────────────────┐
│    Claude Code       │    Gemini CLI        │
│    (Markdown)        │    (TOML)            │
├──────────────────────┼──────────────────────┤
│ YAML frontmatter     │ [command] section    │
│ ```json 코드블록     │ ANSI color codes     │
│ Markdown tables      │ Plain text boxes     │
│ ## 헤더              │ === 구분선           │
└──────────────────────┴──────────────────────┘
```

### 4.4 Context Engineering 기법

#### 4.4.1 선언적 프롬프트 (Declarative Prompt)
```yaml
# pdca-plan.md frontmatter
name: pdca-plan
description: Generate Plan phase document (feature planning)
argument:
  name: feature_name
  description: The name of the feature to plan
  required: true
```

#### 4.4.2 템플릿 참조 (Template Reference)
```markdown
## Template
Use: `templates/plan.template.md`

## Output Path
Save to: `docs/01-plan/features/{feature_name}.plan.md`
```

#### 4.4.3 상태 기반 라우팅 (State-based Routing)
```markdown
# pdca-next.md
## Decision Matrix

| Current State | Next Action |
|---------------|-------------|
| No Plan       | Run /pdca-plan |
| Plan exists   | Run /pdca-design |
| Design exists | Start implementation |
| Do complete   | Run /pdca-analyze |
| Check < 90%   | Run /pdca-iterate |
| Check >= 90%  | Run /pdca-report |
```

---

## Part 5: hooks/ 및 lib/ 분석

### 5.1 hooks/ 개요

| 파일 | 목적 |
|------|------|
| **hooks.json** | 훅 트리거 정의 |
| **session-start.js** | 세션 시작 시 초기화 |

### 5.2 훅 유형

```json
// hooks.json
{
  "hooks": [
    {
      "event": "SessionStart",
      "type": "once",
      "command": "node ${CLAUDE_PLUGIN_ROOT}/hooks/session-start.js"
    },
    {
      "event": "PreToolUse",
      "matcher": "Write|Edit",
      "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/pre-write.js"
    },
    {
      "event": "PostToolUse",
      "matcher": "Write",
      "command": "node ${CLAUDE_PLUGIN_ROOT}/scripts/pdca-post-write.js"
    }
  ]
}
```

### 5.3 session-start.js Context Engineering 기법

| 기법 | 설명 |
|------|------|
| **플랫폼 감지** | Claude Code vs Gemini CLI 자동 감지 |
| **PDCA 상태 초기화** | 기존 작업 상태 로드 및 표시 |
| **Enhanced Onboarding** | 이전 작업 resume 옵션 제공 |
| **다국어 지원** | 8개 언어 트리거 키워드 표시 |

### 5.4 lib/common.js 분석

| 항목 | 내용 |
|------|------|
| 파일 크기 | 2,776줄 |
| 내보내기 함수 | 76개 |
| 섹션 수 | 18개 |

#### 5.4.1 주요 유틸리티 분류

```
┌─────────────────────────────────────────────────────────────────┐
│                    lib/common.js 구조                            │
├───────────────────────┬─────────────────────────────────────────┤
│ Configuration (3)     │ getPluginRoot, getDocsRoot, getConfig   │
├───────────────────────┼─────────────────────────────────────────┤
│ File Detection (4)    │ isCodeFile, isDocFile, isConfigFile...  │
├───────────────────────┼─────────────────────────────────────────┤
│ Tier Detection (7)    │ detectProjectTier, getTierDescription   │
├───────────────────────┼─────────────────────────────────────────┤
│ PDCA Management (11)  │ getPdcaStatus, updatePdcaStatus...      │
├───────────────────────┼─────────────────────────────────────────┤
│ Intent Detection (3)  │ detectIntent, classifyTask, analyzeReq  │
├───────────────────────┼─────────────────────────────────────────┤
│ Ambiguity Detection (9)│ calculateAmbiguity, generateClarify... │
├───────────────────────┼─────────────────────────────────────────┤
│ Platform Compat (8)   │ formatOutput, formatError, formatJson   │
├───────────────────────┼─────────────────────────────────────────┤
│ Debug Logging (3)     │ debugLog, setDebugMode, getDebugMode    │
└───────────────────────┴─────────────────────────────────────────┘
```

#### 5.4.2 핵심 Context Engineering 기법

**의도 감지 (Intent Detection)**
```javascript
// 8개 언어 지원 키워드 매핑
const INTENT_PATTERNS = {
  plan: ['plan', '계획', '計画', '计划', 'planificar', 'planifier', 'planen', 'pianificare'],
  design: ['design', '설계', '設計', '设计', 'diseñar', 'concevoir', 'entwerfen', 'progettare'],
  // ...
};

function detectIntent(message) {
  // 키워드 매칭 및 점수화
}
```

**모호성 감지 (Ambiguity Detection)**
```javascript
function calculateAmbiguityScore(request) {
  let score = 0;

  // 1. 대명사 과다 사용
  if (hasTooManyPronouns(request)) score += 20;

  // 2. 범위 불명확
  if (hasUnclearScope(request)) score += 30;

  // 3. 기술 스택 미명시
  if (missingTechStack(request)) score += 15;

  // 점수 > 50이면 명확화 질문 생성
  return score;
}
```

**캐싱 시스템**
```javascript
// 파일 해시 기반 캐싱
const cache = new Map();

function getCachedOrCompute(key, computeFn, ttl = 60000) {
  const cached = cache.get(key);
  if (cached && Date.now() - cached.timestamp < ttl) {
    return cached.value;
  }
  const value = computeFn();
  cache.set(key, { value, timestamp: Date.now() });
  return value;
}
```

---

## Part 6: scripts/ 디렉토리 분석

### 6.1 개요

| 항목 | 내용 |
|------|------|
| 파일 수 | 26개 |
| 형식 | JavaScript (Node.js) |
| 목적 | Hook 핸들러 및 자동화 스크립트 |

### 6.2 스크립트 분류

```
┌─────────────────────────────────────────────────────────────────┐
│                     scripts/ 분류                                │
├─────────────────────┬───────────────────────────────────────────┤
│ PDCA Hooks (10)     │ pre-write.js, pdca-post-write.js,         │
│                     │ gap-detector-stop.js, iterator-stop.js... │
├─────────────────────┼───────────────────────────────────────────┤
│ Phase Hooks (9)     │ phase1-schema-stop.js ~ phase9-deploy-pre │
├─────────────────────┼───────────────────────────────────────────┤
│ QA Scripts (3)      │ qa-pre-bash.js, qa-monitor-post.js,       │
│                     │ qa-stop.js                                │
├─────────────────────┼───────────────────────────────────────────┤
│ Utilities (4)       │ select-template.js, sync-folders.js,      │
│                     │ validate-plugin.js, archive-feature.js    │
└─────────────────────┴───────────────────────────────────────────┘
```

### 6.3 Context Engineering 기법

#### 6.3.1 Pre-Hook 검증
```javascript
// pre-write.js - 코드 작성 전 검증
module.exports = async function preWrite(context) {
  const { filePath, content } = context;

  // 1. PDCA 문서 존재 확인
  if (isFeatureCode(filePath) && !hasPdcaPlan(filePath)) {
    return {
      decision: 'block',
      message: 'Plan document required before implementation'
    };
  }

  // 2. 컨벤션 위반 체크
  const violations = checkConventions(content);
  if (violations.length > 0) {
    return {
      decision: 'warn',
      message: formatViolations(violations)
    };
  }

  return { decision: 'allow' };
};
```

#### 6.3.2 Post-Hook 상태 업데이트
```javascript
// pdca-post-write.js - 코드 작성 후 PDCA 상태 업데이트
module.exports = async function postWrite(context) {
  const { filePath } = context;

  // 작성된 파일 유형에 따라 PDCA 상태 업데이트
  if (isImplementationFile(filePath)) {
    await updatePdcaStatus({
      phase: 'Do',
      status: 'in_progress',
      lastModified: filePath
    });
  }
};
```

#### 6.3.3 Stop Hook 요약 생성
```javascript
// gap-detector-stop.js - Gap 분석 완료 시 요약
module.exports = async function stop(context) {
  const analysisResults = await readAnalysisResults();

  // Match Rate에 따른 다음 액션 제안
  if (analysisResults.matchRate < 70) {
    return {
      summary: 'Major gaps detected. Immediate fixes required.',
      nextAction: 'Fix critical gaps before proceeding'
    };
  } else if (analysisResults.matchRate < 90) {
    return {
      summary: `Match rate: ${analysisResults.matchRate}%`,
      nextAction: 'Run /pdca-iterate for automatic improvement'
    };
  }

  return {
    summary: 'Analysis complete. Ready for report.',
    nextAction: 'Run /pdca-report'
  };
};
```

#### 6.3.4 QA Pre-Bash 필터링
```javascript
// qa-pre-bash.js - 위험한 명령 차단
const BLOCKED_COMMANDS = [
  /rm\s+-rf/,
  /docker\s+system\s+prune/,
  /kubectl\s+delete\s+--all/
];

module.exports = async function preBash(context) {
  const { command } = context;

  for (const pattern of BLOCKED_COMMANDS) {
    if (pattern.test(command)) {
      return {
        decision: 'block',
        message: `Destructive command blocked: ${command}`
      };
    }
  }

  return { decision: 'allow' };
};
```

---

## Part 7: skills/ 디렉토리 분석

### 7.1 개요

| 항목 | 내용 |
|------|------|
| 폴더 수 | 18개 |
| 형식 | Markdown (SKILL.md) with YAML frontmatter |
| 목적 | 도메인별 전문 지식 제공 |

### 7.2 스킬 분류

```
┌─────────────────────────────────────────────────────────────────┐
│                       skills/ 분류                               │
├───────────────────────┬─────────────────────────────────────────┤
│ Level Skills (3)      │ starter, dynamic, enterprise            │
├───────────────────────┼─────────────────────────────────────────┤
│ Pipeline Skills (10)  │ development-pipeline, phase-1~9         │
├───────────────────────┼─────────────────────────────────────────┤
│ Platform Skills (2)   │ mobile-app, desktop-app                 │
├───────────────────────┼─────────────────────────────────────────┤
│ Core Skills (2)       │ bkit-rules, bkit-templates              │
├───────────────────────┼─────────────────────────────────────────┤
│ QA Skills (1)         │ zero-script-qa                          │
└───────────────────────┴─────────────────────────────────────────┘
```

### 7.3 스킬별 Context Engineering 기법

#### 7.3.1 bkit-rules (핵심 규칙)

**SoR (Source of Record) 우선순위**
```markdown
## Source of Record Priority

1. **Code** (최우선): 실제 구현이 진실
2. **CLAUDE.md**: 프로젝트별 규칙
3. **docs/**: 설계 문서

충돌 시: Code > CLAUDE.md > docs
```

**태스크 분류 기준**
```markdown
| Task Size | Character Count | PDCA Level |
|-----------|-----------------|------------|
| Quick Fix | < 100           | Minimal    |
| Minor     | 100-500         | Brief      |
| Feature   | 500-2000        | Standard   |
| Major     | > 2000          | Full       |
```

#### 7.3.2 starter (초급 레벨)

**기술 선택 분기**
```markdown
## Tech Stack Options

### Option A: Pure HTML/CSS/JS
- For: 완전 초보자
- Structure: index.html, style.css, script.js

### Option B: Next.js + Tailwind
- For: 프레임워크 경험자
- Structure: app/, components/, public/
```

**흔한 실수 테이블**
```markdown
| Problem | Solution |
|---------|----------|
| CSS not loading | Check file path |
| JS not working | Check console errors |
| Image not showing | Use correct relative path |
```

#### 7.3.3 enterprise (고급 레벨)

**Clean Architecture 4계층**
```markdown
┌─────────────────────────────────────────────────────────────────┐
│                    Clean Architecture                            │
├─────────────────────────────────────────────────────────────────┤
│ API Layer        │ Controllers, Routes, Middleware             │
├─────────────────────────────────────────────────────────────────┤
│ Application Layer│ Use Cases, DTOs, Services                   │
├─────────────────────────────────────────────────────────────────┤
│ Domain Layer     │ Entities, Value Objects, Domain Events      │
├─────────────────────────────────────────────────────────────────┤
│ Infrastructure   │ Repositories, External Services             │
└─────────────────────────────────────────────────────────────────┘
```

**AI Native 개발 10일 패턴**
```markdown
Day 1-2: Schema + Convention (Phase 1-2)
Day 3-4: Mockup + API Design (Phase 3-4)
Day 5-6: Design System + UI (Phase 5-6)
Day 7-8: SEO/Security + Review (Phase 7-8)
Day 9-10: Deployment + Documentation (Phase 9)
```

#### 7.3.4 zero-script-qa

**JSON 로그 표준**
```json
{
  "timestamp": "ISO 8601 format",
  "level": "DEBUG|INFO|WARNING|ERROR",
  "service": "api|web|nginx|worker",
  "request_id": "req_xxxxxxxx",
  "message": "Log message",
  "data": { "additional": "data" }
}
```

**이슈 탐지 임계값**
```markdown
| Severity | Condition | Action |
|----------|-----------|--------|
| Critical | level: ERROR | Immediate report |
| Critical | status: 5xx | Immediate report |
| Critical | duration_ms > 3000 | Immediate report |
| Warning  | status: 401, 403 | Warning report |
| Warning  | duration_ms > 1000 | Warning report |
```

---

## Part 8: templates/ 디렉토리 분석

### 8.1 개요

| 항목 | 내용 |
|------|------|
| 파일 수 | 20개 |
| 형식 | Markdown templates with placeholders |
| 목적 | PDCA/Pipeline 문서 스캐폴딩 |

### 8.2 템플릿 분류

```
┌─────────────────────────────────────────────────────────────────┐
│                     templates/ 분류                              │
├───────────────────────┬─────────────────────────────────────────┤
│ PDCA Core (7)         │ plan, design, design-starter,           │
│                       │ design-enterprise, analysis, report,     │
│                       │ iteration-report                        │
├───────────────────────┼─────────────────────────────────────────┤
│ Pipeline (10)         │ phase-1~9, zero-script-qa               │
├───────────────────────┼─────────────────────────────────────────┤
│ Auxiliary (3)         │ _INDEX, CLAUDE, TEMPLATE-GUIDE          │
└───────────────────────┴─────────────────────────────────────────┘
```

### 8.3 Context Engineering 기법

#### 8.3.1 플레이스홀더 시스템

```markdown
## Placeholder Syntax

Markdown style: {feature}, {date}, {author}
Mustache style: {{FEATURE_NAME}}, {{DATE}}
Handlebars style: {{#if TIER_1}}...{{/if}}
```

#### 8.3.2 Cross-Reference 시스템

```markdown
## Pipeline References

| Document | Path | Status |
|----------|------|--------|
| Phase 1 Schema | docs/pipeline/phase-1-schema.md | ✅ |
| Phase 2 Convention | docs/pipeline/phase-2-convention.md | ✅ |
| Phase 4 API | docs/pipeline/phase-4-api.md | 🔄 |
```

#### 8.3.3 ASCII 다이어그램

```markdown
## Score Visualization

┌─────────────────────────────────────────────┐
│                                             │
│   Overall Score: 87%                        │
│   ████████████████████░░░░ 87/100           │
│                                             │
│   Design Match:  █████████░ 90%             │
│   Code Quality:  ████████░░ 80%             │
│   Security:      █████████░ 85%             │
│                                             │
└─────────────────────────────────────────────┘
```

#### 8.3.4 레벨별 템플릿 분기

```markdown
## Template Selection

| Level      | Design Template           |
|------------|---------------------------|
| Starter    | design-starter.template   |
| Dynamic    | design.template (default) |
| Enterprise | design-enterprise.template|
```

---

## Part 9: Context Engineering 기법 종합 매트릭스

### 9.1 기법별 적용 현황

| 기법 | agents | commands | hooks/lib | scripts | skills | templates |
|------|--------|----------|-----------|---------|--------|-----------|
| Role Definition | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ |
| System Prompts | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |
| Few-shot Examples | ✅ | ✅ | ❌ | ❌ | ✅ | ✅ |
| Chain-of-Thought | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ |
| Tool Design | ✅ | ❌ | ❌ | ❌ | ✅ | ❌ |
| Memory Management | ❌ | ❌ | ✅ | ✅ | ❌ | ❌ |
| Multi-language | ✅ | ✅ | ✅ | ❌ | ✅ | ❌ |
| Threshold-based | ✅ | ✅ | ✅ | ✅ | ✅ | ✅ |
| Template System | ❌ | ✅ | ❌ | ❌ | ❌ | ✅ |
| Hook Integration | ✅ | ❌ | ✅ | ✅ | ✅ | ❌ |
| Caching | ❌ | ❌ | ✅ | ❌ | ❌ | ❌ |
| State Management | ❌ | ✅ | ✅ | ✅ | ❌ | ❌ |

### 9.2 영역별 강점

| 영역 | 주요 강점 |
|------|-----------|
| **agents/** | 역할 분리, 모델 선택 최적화, 도구 제한 |
| **commands/** | 선언적 정의, 플랫폼 호환성, 워크플로우 통합 |
| **hooks/lib** | 이벤트 기반 확장, 캐싱, 의도 감지 |
| **scripts/** | Pre/Post 검증, 상태 자동 업데이트, 요약 생성 |
| **skills/** | 도메인 지식 구조화, 레벨별 적응, 다국어 지원 |
| **templates/** | 문서 일관성, Cross-reference, 시각화 |

---

## Part 10: 개선 기회 및 권장사항

### 10.1 현재 Gap 분석

| 영역 | Gap | 영향 | 우선순위 |
|------|-----|------|----------|
| **RAG 통합** | 외부 문서 검색 미구현 | 대규모 코드베이스 지원 제한 | High |
| **동적 토큰 관리** | 컨텍스트 크기 모니터링 부재 | 긴 세션에서 성능 저하 가능 | High |
| **Few-shot 라이브러리** | 예제가 인라인으로만 존재 | 재사용 및 관리 어려움 | Medium |
| **성능 메트릭** | 토큰 사용량 추적 부재 | 최적화 기회 파악 어려움 | Medium |
| **테스트 커버리지** | 스크립트 테스트 부재 | 리팩토링 시 회귀 위험 | Low |

### 10.2 권장 개선사항

#### 10.2.1 RAG 시스템 도입 (High Priority)

```markdown
## 제안: 코드베이스 RAG 통합

1. Vector DB 연동 (Pinecone, Weaviate)
2. 코드 임베딩 생성 파이프라인
3. 시맨틱 검색 도구 추가
4. Just-in-time 컨텍스트 로딩
```

#### 10.2.2 동적 컨텍스트 관리 (High Priority)

```javascript
// 제안: 토큰 카운터 미들웨어
function contextManager(context) {
  const tokenCount = countTokens(context);

  if (tokenCount > THRESHOLD) {
    return compactContext(context, {
      keepRecent: 10,
      summarizeOld: true
    });
  }

  return context;
}
```

#### 10.2.3 Few-shot 라이브러리 (Medium Priority)

```yaml
# 제안: examples/library.yaml
examples:
  code_review:
    - input: "Review this function"
      output: "1. Function purpose: ...\n2. Issues found: ..."

  gap_analysis:
    - input: "Compare design vs implementation"
      output: "Match rate: 85%\n..."
```

### 10.3 향후 로드맵

```
┌─────────────────────────────────────────────────────────────────┐
│                     개선 로드맵                                   │
├─────────────────────────────────────────────────────────────────┤
│ Phase 1 (Q1 2026)                                               │
│   - RAG 시스템 프로토타입                                        │
│   - 토큰 카운터 구현                                             │
│   - 성능 메트릭 대시보드                                         │
├─────────────────────────────────────────────────────────────────┤
│ Phase 2 (Q2 2026)                                               │
│   - Few-shot 라이브러리 구축                                     │
│   - Compaction 전략 고도화                                       │
│   - 다중 에이전트 오케스트레이션                                 │
├─────────────────────────────────────────────────────────────────┤
│ Phase 3 (Q3 2026)                                               │
│   - MCP 서버 통합 확대                                           │
│   - 자동 컨텍스트 최적화                                         │
│   - 엔터프라이즈 기능 강화                                       │
└─────────────────────────────────────────────────────────────────┘
```

---

## Part 11: 결론

### 11.1 핵심 발견사항 요약

bkit 플러그인은 **Context Engineering**의 핵심 원칙을 체계적으로 구현하고 있습니다:

1. **역할 기반 에이전트 분리**: 11개 전문 에이전트가 각자의 도메인에 특화
2. **선언적 커맨드 시스템**: PDCA/Pipeline 워크플로우의 일관된 실행
3. **이벤트 기반 훅**: Pre/Post 검증으로 품질 자동 관리
4. **도메인 지식 구조화**: 18개 스킬로 레벨/플랫폼별 맞춤 지식 제공
5. **문서 템플릿 시스템**: 20개 템플릿으로 일관된 문서화 지원

### 11.2 bkit의 Context Engineering 성숙도

```
┌─────────────────────────────────────────────────────────────────┐
│           bkit Context Engineering 성숙도 평가                   │
├─────────────────────────┬───────────────────────────────────────┤
│ System Prompts          │ ████████████████████ 100%            │
│ Tool Design             │ █████████████████░░░  85%            │
│ Few-shot Prompting      │ ████████████████░░░░  80%            │
│ Memory Management       │ ████████████░░░░░░░░  60%            │
│ RAG Integration         │ ████░░░░░░░░░░░░░░░░  20%            │
│ Dynamic Context Mgmt    │ ██████░░░░░░░░░░░░░░  30%            │
├─────────────────────────┼───────────────────────────────────────┤
│ Overall Maturity        │ ████████████████░░░░  70%            │
└─────────────────────────┴───────────────────────────────────────┘
```

### 11.3 최종 평가

bkit은 **AI-Native 개발 워크플로우**를 위한 강력한 Context Engineering 프레임워크입니다. PDCA 방법론과 9-Phase Pipeline을 통해 체계적인 개발 프로세스를 제공하며, 다국어 지원과 레벨별 적응을 통해 다양한 사용자를 지원합니다.

향후 RAG 통합과 동적 컨텍스트 관리 기능이 추가되면, 대규모 엔터프라이즈 프로젝트에서도 효과적으로 활용될 수 있을 것으로 기대됩니다.

---

## 참고 자료

### Context Engineering 공식 자료
- [Anthropic: Effective Context Engineering for AI Agents](https://www.anthropic.com/engineering/effective-context-engineering-for-ai-agents)
- [Prompt Engineering Guide](https://www.promptingguide.ai/)

### bkit 관련 자료
- [bkit GitHub Repository](https://github.com/popup-studio-ai/bkit-claude-code)
- [Claude Code Documentation](https://docs.claude.ai/en/claude-code)
- [Gemini CLI Documentation](https://ai.google.dev/gemini-api/docs/gemini-cli)

### 2025 트렌드
- [Agentic Context Engineering: The Complete 2025 Guide](https://www.sundeepteki.org/blog/agentic-context-engineering)
- [Context Engineering for Reliable AI Agents](https://www.kubiya.ai/blog/context-engineering-ai-agents)
- [ArXiv: Agentic Context Engineering (ACE)](https://arxiv.org/abs/2510.04618)

---

> **Document Version**: 1.0.0
> **Last Updated**: 2026-01-25
> **Generated by**: Claude Opus 4.5 with bkit plugin
